# Velox Orchestrator - Web Interface

Веб-интерфейс для управления платформой Velox Orchestrator. Разработан на SvelteKit + TailwindCSS.

## 🛠 Технологии
- **SvelteKit**: Фреймворк для SPA/SSR.
- **TailwindCSS**: Утилитарная стилизация.
- **Vite**: Сборщик.

## 🚀 Запуск в режиме разработки

1. Перейдите в папку `front`:
   ```bash
   cd front
   ```

2. Установите зависимости (если еще не установлены):
   ```bash
   npm install --legacy-peer-deps
   ```

3. Разрешите выполнение скриптов (опционально, если есть проблемы с запуском):
   ```bash
   npm run dev -- --host
   ```
   Интерфейс будет доступен по адресу: `http://localhost:5173`

## 🔑 Авторизация
Для входа используйте учетные данные, указанные в переменных окружения backend'а (или файле конфигурации):
- **User**: `admin` (по умолчанию)
- **Password**: `admin` (по умолчанию)

## 📦 Сборка для продакшена

```bash
npm run build
```
Результат сборки будет в папке `build/`.

## ⚙️ Конфигурация
URL API можно задать через переменную окружения `VITE_API_URL` в файле `.env` (в корне папки `front`). По умолчанию: `http://localhost:8000`.
